Instale a biblioteca:

```powershell
pip install yt-dlp
```

Para converter para MP3, instale tambem o FFmpeg:

```powershell
winget install Gyan.FFmpeg
```

O mais simples é rodar direto pelo terminal Python. Caso não queira, execute:

```powershell
python main.py
```